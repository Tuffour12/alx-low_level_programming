#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: Write a code that print a number in base 10
 *
 * Return: Always 0
 */
int main(void)
{
	int n;

	for (n = 0; n <= 9; n++)
	{
		printf(n);
	}
	printf('\n');

	return (0);
}
